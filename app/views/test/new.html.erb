<%= form_for @test, url: {action: :create} do |f| %>   
        
        <h3>Add a Test</h3>   
  
        <div class="field">   
            <%= f.label :name %>   
              <%= f.text_field :name %>   
        </div>   
        <div class="field"> 
              <%= f.number_field :course_id, id: :courses_test_course_id,value: params[:course_id], type: :hidden%> 
        </div>  
        </p>   
      </div>   
       <div class="actions">   
        <%= link_to 'Back', course_test_index_path %>
        <%= f.submit %>   
      </div>   
  <% end %>   
</div>